// RULE: bundle-dynamic-features + bundle-dom-animation
// Выносим в отдельный файл для динамического импорта с code-splitting
import { domAnimation } from 'framer-motion';

export default domAnimation;